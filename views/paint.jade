

script.status-template(type="x-handlebars-template")
  .attr
    span.name Status:
    span.value  {{status}}
  .attr
    span.name Loaded:
    span.value  {{drawnFromHistory}} / {{inHistory}} operations in {{historyDrawTime}}s
  .attr
    span.name Operations drawn:
    span.value  {{operationCount}}


h1

.status

.tool_settings

  .top

    .color
      button.selected(data-color="black") Black
      button(data-color="white") White
      button(data-color="red") Red
      button(data-color="green") Green
      button(data-color="blue") Blue
      button(data-color="yellow") Yellow
      button(data-color="brown") Brown

  .left

    .tool
      button.selected(data-tool="Pencil") Pencil
      button(data-tool="Eraser") Eraser

    .size
      input.size-value(type="text", value="50", size="2")
      span px
      div
        button.smaller &lt;
        button.bigger &gt;




.loading
  p Loading...

.whiteboard
  canvas.sketch(width="1000", height="600") Your browser does not support canvas
  canvas.remoteSketch(width="1000", height="600") Your browser does not support canvas
  canvas.main(width="1000", height="600") Your browser does not support canvas




!{renderScriptTags("paint")}
