

script.status-template(type="x-handlebars-template")
  .attr
    span.name Status:
    span.value  {{status}}
  .attr
    span.name Cached:
    span.value  {{cachedDraws}} draws
  .attr
    span.name Start:
    span.value  {{startDraws}} draws
  .attr
    span.name You:
    span.value  {{userDraws}} draws
  .attr
    span.name Remote:
    span.value  {{remoteDraws}} draws
  .attr
    span.name Total:
    span.value  {{totalDraws}} draws


h1

.status



.tool_settings

  .top

    .color
      button.selected(data-color="black")
      button(data-color="white")
      button(data-color="green")
      button(data-color="blue")
      button(data-color="yellow")
      button(data-color="brown")
      button(data-color="red")
      button(data-color="purple")

  .left

    .tool
      button.selected(data-tool="Pencil") Pen
      button(data-tool="Line") Line
      button(data-tool="Eraser") Eraser
      button(data-tool="Circle") Circle

    .size
      input.size-value(type="text", value="10", size="2")
      span px
      div
        button.smaller &lt;
        button.bigger &gt;

.loading
  p Loading...

.whiteboard
  canvas.loading.buffer Your browser does not support canvas
  canvas.loading.main Your browser does not support canvas




!{renderScriptTags("paint")}
